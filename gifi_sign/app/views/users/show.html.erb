<% provide(:title, @user.name) %>

<br>

<div class="col-sm-10 well container" style="margin-left: 8%">
	<%= gravatar_for @user %>
	<h1 class="titulo"><%= @user.name %></h1>
	<br>
	<%= @user.name %>, <%= @user.email %>
</div>

<br>

<div class="col-sm-10 well" style="margin-left: 8%">
	<div class="row">
		<div class="col-md-2" align="right">
			<%= gravatar_for @user %>
		</div>
		<div class="col-md-10">
			<h1 class="titulo"><%= @user.name %></h1>
			<br>
			<%= @user.name %>, <%= @user.email %>
		</div>
</div>
